/*
 * Name:	MarkerConfig
 * Date:	2017-11-25
 * Version: 1.0
 * Author:  Engima
 *
 * Description:
 * Contains information about all markers that need to be for each side.
 */
using Intrusion.Common;

namespace Intrusion.Server
{
	public class MapMarkersConfig
	{
		private static fields ["_mSideInfos" as Array];
	
		// Initializes a MissionConfig object.
		public static method Init {
			_mSideInfos = [
				[west, "respawn_west", "INT_CarParkWest", "\A3\Data_F\Flags\Flag_NATO_CO.paa"] new SideMarkersInfo,
				[east, "respawn_east", "INT_CarParkEast", "\A3\Data_F\Flags\Flag_CSAT_CO.paa"] new SideMarkersInfo
			];
			
			_self.WinnersLocationMarkerType = "hd_flag";
		};
		
		// Gets the marker type of the winners marker type.
		public static property String WinnersLocationMarkerType { get; private set; };
		
		// Gets a mission side info config object.
		// _side (Side): The side of the config object.
		// Returns (MissionSideInfo): A mission side config object.
		public static method GetSideMarkerInfo {
			params ["_side" as Side];
			private ["_info" as SideMarkersInfo];
			[str _this + " MapMarkersInfo.SideMarkersInfo"] call LogHandler.Debug;

			scopeName "main";
			_info = classNull;
			
			{
				if (_x.Side == _side) then {
					_x breakOut "main";
				};
			} foreach _mSideInfos as SideMarkersInfo;
			
			_info
		};
	};
};
