/*
 * Name:	TestWorld
 * Date:	2019-05-17
 * Version: 1.0
 * Author:  Engima
 *
 * Description:
 * A fake world class used in testing.
 */

using Intrusion.Ai;

namespace Intrusion.Tests.Helpers
{
	public class TestWorld : IWorld
	{
		// Creates a TestWorld object.
		public constructor {
			_self.LeaderPos = [];
			_self.Actions = [];
		};
		
		public property Array Actions { get; private set; };
		
		public property Array LeaderPos { get; set; };
		
		// Gets a group's leader's position.
		// _group (Group): The group to get the leader's position from.
		// Returns (Array): The position of the leader.
		public method Array GetLeaderPos("_group" as Group)
		{
			_self.LeaderPos
		};
		
		// Performs a move action for an AI group.
		// _group (Group): The group to move.
		// _moveAction (MoveAction): Information about the movement.
		public method DoMove("_group" as Group, "_moveAction" as MoveAction)
		{
			_self.Actions pushBack _moveAction;
		};
	};
};
